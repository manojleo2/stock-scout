name: Daily Stock Scout Telegram Alerts
on:
  schedule:
    - cron: '15 3 * * 1-5'
    - cron: '0 4 * * 1-5'
    - cron: '15 10 * * 1-5'
  workflow_dispatch:
    inputs:
      mode:
        type: choice
        description: 'Cron Execution Mode'
        options:
          - pre_market
          - intraday
          - post_market
        default: 'pre_market'

jobs:
  run-alerts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy scikit-learn requests feedparser nltk lxml
          python -c "import nltk; nltk.download('vader_lexicon')"

      - name: Determine execution mode
        run: |
          MODE="${{ github.event.inputs.mode }}"
          if [ -z "$MODE" ]; then
            MODE="pre_market"
          fi
          echo "SELECTED_MODE=$MODE" >> $GITHUB_ENV

      - name: Execute Alert Runner
        env:
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: python utils/cron_runner.py ${{ env.SELECTED_MODE }}
